```json
{
  "name": "Wen",
  "role": "Marketing data analyst",
  "clarity": "Yes",
  "clarity_reason": "Headline 'Turn your tracker export into a weekly status — in seconds' plus the two clearly-styled tabs (Weekly Status / Sprint Review) and 'Drop a Jira, Linear, Asana, or GitHub CSV' told me exactly what it is and that there are two modes within ~5 seconds. 'Your file never leaves your browser — no upload, no signup' immediately earned trust, which matters to me.",
  "value": "Yes",
  "value_reason": "I build recurring stakeholder progress reports from CSV. The Sprint Review tab shows all four things I need: VELOCITY (21 of 34 pts shipped, 4 of 7 issues), SCOPE CHANGE (+8/-2 mid-sprint), SPILLOVER (3 issues/13 pts, named items + a 'carry-over' badge on Sprint 23), and a BY ASSIGNEE points breakdown. Sprint selector recomputes correctly per sprint. Critically for me: when I dropped my OWN marketing CSV with a non-standard status 'Awaiting Stakeholder Sign-off', it did NOT silently mis-bucket it as done (8 of 21 pts shipped was correct) and it printed 'Scope change unavailable — needs a Sprint + Added-date column' instead of faking a number. That honesty about data gaps is exactly what I distrust most tools for not doing. Copy Markdown and Copy plain text both matched the screen exactly, value-for-value. Today I do this by hand in Sheets pivoting a Jira export — this is a real time save.",
  "advocacy": 8,
  "advocacy_reason": "Genuinely useful, transparent with data, no signup, copies clean Markdown I can paste into Slack/a doc. Not a 9-10 because one number could mislead a stakeholder: the BY ASSIGNEE points (e.g. Sam 18 pts, totaling 34) appear to be COMMITTED points, while the velocity headline says only 21 shipped — a reader could read 'Sam — 18 pts' as 18 delivered. For a stakeholder report that ambiguity is dangerous and I'd have to caveat it manually. Also the Copy buttons are <span>, not <button> (minor a11y), and inline edit commits via a 'Save' button only — I instinctively hit Enter and thought it was broken.",
  "top_fix": "In BY ASSIGNEE, label/split the points as shipped vs committed (e.g. 'Sam — 12 of 18 pts shipped') so the per-person breakdown reconciles with the velocity headline; right now totals match committed, not shipped, and that can mislead a stakeholder.",
  "notes": "No console/page errors at any point. Sprint selector (24/23/22) works and recomputes velocity/scope/spillover. Sprint 23 legitimately shows '0 of 9 shipped, all spillover' with a 'carry-over' badge — good. Inline 'Edit line' opens an editable field with a Save button (works; Enter does not commit — friction). Copy Markdown and Copy plain text both reproduced the on-screen numbers and lists exactly (verified clipboard read). My own 5-row CSV parsed correctly, correctly excluded an unknown status from 'done', and gave an honest 'scope change unavailable' message instead of a fabricated value. UNMAPPED STATUS section on the Weekly tab (with a 'Move to...' dropdown) is a strong trust feature too."
}
```

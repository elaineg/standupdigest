# Dana — Round 3

**Persona:** Demand-gen lead, runs a small Asana team, owes a weekly stakeholder status. Ruthless about time; value must land in one scroll.

## PRIOR BLOCKER — RESOLVED? YES.
Round-2 killer: Asana's real `Task Name` header wasn't auto-detected → every row showed "(untitled)" until a manual Remap. I built a true Asana export (`Task Name,Assignee,Section/Column,Due Date,Completed At,Tags`) and uploaded it. **All 6 task titles rendered correctly on first parse — zero "(untitled)", zero manual remap.** Statuses auto-mapped to Shipped (3) / In Progress (2) / Blocked (1) with "All statuses recognized ✓". "Remap columns" is now just an optional link, not a forced warning panel. This is the one-scroll experience I wanted.

## (1) CLARITY — Yes
Same strong landing in 5s: "Turn your tracker export into a weekly status — in seconds", lists Jira/Linear/Asana/GitHub, "no upload, no signup."

## (2) VALUE — Yes
Drop my actual Asana export → exec-ready digest in one scroll. Copied Markdown AND plain text are clean and grouped by assignee — both verified from the real clipboard, no phantom-name parens in the copy. Edit affordances present. This replaces my ~20 min Friday Notion/Canva hand-massage.

## (3) ADVOCACY — 8/10
Up to a confident 8. The real-Asana-data snag that capped me last round is gone, so I'd now screenshot this to the team channel. Still not a 9 because: (a) on-screen redundant "(Sam)" parens under "👤 Sam" grouping persist — cosmetic, copy is clean; (b) it's still per-export, no saved/recurring weekly view — I re-export from Asana every week with no memory of last week's setup. A "remember my columns/last file" would push this to 9.

## Residual hesitations
- On-screen "(assignee)" parens duplicate the group header (copy output is correct).
- No persistence across weeks — re-upload each Friday; column auto-detect now makes that cheap, but a saved view would seal it.
- Did not re-verify the round-2 "All dates" tense nit this round (focused on the Task Name fix); flagging it may still read "This week..." in all-dates scope.

```json
{"tester": 5, "round": 3, "clarity": "Yes", "value": "Yes", "advocacy": 8, "topComplaints": ["No saved/recurring weekly view — re-upload + re-detect every Friday with no memory", "On-screen redundant '(assignee)' parens under assignee grouping (copy is clean)"], "priorConcernsAddressed": "all"}
```
